# Prompt Library

Reusable prompts, rules, and agents organized under `modules/`. Spec-driven modules (`spec_driven.*`) are tool- and repo-agnostic; pair them with `languages.*` or local rules for stack-specific detail.
