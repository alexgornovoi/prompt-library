---
description: Project-specific naming and whitespace preferences (use a linter for formatting)
globs: "**/*"
alwaysApply: true
---

# Style & Naming

Project preferences—focused on conventions not enforced by linters.

- **File naming:** snake_case (`data_loader.py`).
- **Whitespace:** One blank line between logical blocks; two between top-level classes/functions.
- **Comments:** Focus on "why," not "what." Omit redundant comments.
- **Conciseness:** Prefer `user` over `user_object_from_api`. Avoid over-specific names.
- **Docstrings:** Keep brief. List params and return types.
- **General:** Simple, readable code. Modern syntax (optional chaining, f-strings).
